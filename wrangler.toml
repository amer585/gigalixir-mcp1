name = "gigalixir-mcp"
main = "src/worker.js"
compatibility_date = "2024-01-01"
compatibility_flags = []

[vars]
# Add non-sensitive variables here if needed.
# Secrets (GIGALIXIR_EMAIL, GIGALIXIR_API_KEY) must be set via:
#   wrangler secret put GIGALIXIR_EMAIL
#   wrangler secret put GIGALIXIR_API_KEY
# OR via the Cloudflare Dashboard → Workers → gigalixir-mcp → Settings → Variables
